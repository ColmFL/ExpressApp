var secret = prompt("Please enter the secret word", "Type here");
var secret = Bill Cosby

for (var i=0; i<10; i++){
	var guess = prompt("Enter guess", "Guess");
	if(guess == secret);
	
alert("You are correct!");
}

